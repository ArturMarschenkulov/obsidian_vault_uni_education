It is a [[Convergence tests]].

$$L=\lim_{n \to \infty}|\frac{a_{n+1}}{a_n}|$$
It leads to:
- if $L < 1$ then series converges absolutely
- if $L>1$ then series diverges
- if $L=1$ or there is no limit, then the test is inconclusive